let spinner = document.querySelector('.spinner');
let spin = document.querySelector('.spinner-border text-secondary');
let span = document.querySelector('.sr-only');

window.onload = function(){
    spinner.classList.add('hide');
    spinner.remove();
}